import { decisionFixture as test } from "../decision.fixture";

// TODO: Add tests
test.describe("ClassificationComponent", () => {
  test.beforeEach(async ({ fixture }) => {
    await fixture.create();
  });
});
