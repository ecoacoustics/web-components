import { test } from "../../tests/assertions";

test("should correctly add a single stylesheet", () => {});

// In this test I test passing in an array of stylesheets that has a nested
// array inside it.
// This is possible because each CSSResultArray item can be a CSSResultArray
test("should correctly add an array of stylesheets", () => {});

test("should have no operation if an empty array is passed", () => {});
