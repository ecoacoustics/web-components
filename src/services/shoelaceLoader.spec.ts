import { test } from "../tests/assertions";

test("should register a default icon library correctly", () => {});

test("should have inline svg icons registered in the default icon library", () => {});
