* text=auto eol=lf
*.png filter=lfs diff=lfs merge=lfs -text
*.jpg filter=lfs diff=lfs merge=lfs -text
*.jpeg filter=lfs diff=lfs merge=lfs -text
*.bmp filter=lfs diff=lfs merge=lfs -text
*.svg filter=lfs diff=lfs merge=lfs -text
*.gif filter=lfs diff=lfs merge=lfs -text
*.wav filter=lfs diff=lfs merge=lfs -text
*.flac filter=lfs diff=lfs merge=lfs -text
*.mp3 filter=lfs diff=lfs merge=lfs -text

# mock and testing datasets commonly use csv, tsv, and json format
# because these files can be large, we use LFS to store them
*.csv filter=lfs diff=lfs merge=lfs -text
*.tsv filter=lfs diff=lfs merge=lfs -text
public/*.json filter=lfs diff=lfs merge=lfs -text

# We don't use git lfs for the labels.csv file because it is text-based and
# unlikely to change, meaning that it would not benefit from LFS storage.
public/labels.csv filter= diff= merge=text
